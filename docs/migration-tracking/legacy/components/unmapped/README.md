# Unmapped Components

No unmapped/non-linked component projects remain.

- Scope checked: `legacy/**/*.csproj`, `legacy/**/*.sqlproj`, and all active `legacy/**/*.sln`
- Current state: every tracked project is linked to at least one solution card
