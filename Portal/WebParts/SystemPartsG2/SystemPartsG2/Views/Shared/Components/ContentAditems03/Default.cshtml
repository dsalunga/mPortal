@model WCMS.WebSystem.WebParts.G2.ViewComponents.ContentAditems03ViewModel
@* Ported from content_aditems_03.ascx (AppBundle2/Ads). *@

<div class="component-contentaditems03">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>content aditems 03</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            content aditems 03 component requires configuration.
        </div>
    }
</div>
