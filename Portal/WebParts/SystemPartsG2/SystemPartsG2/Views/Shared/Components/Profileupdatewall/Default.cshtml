@model WCMS.WebSystem.WebParts.G2.ViewComponents.ProfileupdatewallViewModel
@* Ported from ProfileUpdateWall.ascx (AppBundle2/Social/Plugins). *@

<div class="component-profileupdatewall">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Profile Update Wall</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Profile Update Wall component requires configuration.
        </div>
    }
</div>
