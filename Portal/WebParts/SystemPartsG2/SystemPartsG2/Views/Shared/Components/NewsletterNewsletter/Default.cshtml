@model WCMS.WebSystem.WebParts.G2.ViewComponents.NewsletterNewsletterViewModel
@* Ported from Newsletter.ascx (AppBundle2/Newsletter). *@

<div class="component-newsletternewsletter">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Newsletter</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Newsletter component requires configuration.
        </div>
    }
</div>
