@model WCMS.WebSystem.WebParts.G2.ViewComponents.NewsletterAdminnewsletterViewModel
@* Ported from AdminNewsletter.ascx (AppBundle2/Newsletter). *@

<div class="component-newsletteradminnewsletter">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Newsletter</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Admin Newsletter component requires configuration.
        </div>
    }
</div>
