@model WCMS.WebSystem.WebParts.G2.ViewComponents.AdsContentAdcategories01ViewModel
@* Ported from content_adcategories_01.ascx (AppBundle2/Ads). *@

<div class="component-adscontentadcategories01">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>content adcategories 01</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            content adcategories 01 component requires configuration.
        </div>
    }
</div>
