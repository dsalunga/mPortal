@model WCMS.WebSystem.WebParts.G2.ViewComponents.ItemtemplatesTwocolumnsViewModel
@* Ported from TwoColumns.ascx (AppBundle2/BasicList/ItemTemplates). *@

<div class="component-itemtemplatestwocolumns">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Two Columns</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Two Columns component requires configuration.
        </div>
    }
</div>
