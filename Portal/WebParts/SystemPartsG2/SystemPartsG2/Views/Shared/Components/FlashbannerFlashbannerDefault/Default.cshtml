@model WCMS.WebSystem.WebParts.G2.ViewComponents.FlashbannerFlashbannerDefaultViewModel
@* Ported from Default.aspx (AppBundle2/FlashBanner). *@

<div class="component-flashbannerflashbannerdefault">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Default</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Default component requires configuration.
        </div>
    }
</div>
