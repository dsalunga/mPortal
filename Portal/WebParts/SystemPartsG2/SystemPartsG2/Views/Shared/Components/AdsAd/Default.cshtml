@model WCMS.WebSystem.WebParts.G2.ViewComponents.AdsAdViewModel
@* Ported from Ad.ascx (AppBundle2/Ads). *@

<div class="component-adsad">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Ad</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Ad component requires configuration.
        </div>
    }
</div>
