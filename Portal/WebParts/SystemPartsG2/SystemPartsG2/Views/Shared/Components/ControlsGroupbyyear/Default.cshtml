@model WCMS.WebSystem.WebParts.G2.ViewComponents.ControlsGroupbyyearViewModel
@* Ported from GroupByYear.ascx (AppBundle2/Download/Controls). *@

<div class="component-controlsgroupbyyear">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Group By Year</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Group By Year component requires configuration.
        </div>
    }
</div>
