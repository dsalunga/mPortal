@model WCMS.WebSystem.WebParts.G2.ViewComponents.SubsiteslistViewModel
@* Ported from subsiteslist.ascx (AppBundle2/SiteList). *@

<div class="component-subsiteslist">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>subsiteslist</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            subsiteslist component requires configuration.
        </div>
    }
</div>
