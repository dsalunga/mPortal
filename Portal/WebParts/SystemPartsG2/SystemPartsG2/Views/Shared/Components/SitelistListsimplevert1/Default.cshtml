@model WCMS.WebSystem.WebParts.G2.ViewComponents.SitelistListsimplevert1ViewModel
@* Ported from listsimplevert1.ascx (AppBundle2/SiteList). *@

<div class="component-sitelistlistsimplevert1">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>listsimplevert1</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            listsimplevert1 component requires configuration.
        </div>
    }
</div>
