@model WCMS.WebSystem.WebParts.G2.ViewComponents.SitelistSubsiteslistNbViewModel
@* Ported from subsiteslist_nb.ascx (AppBundle2/SiteList). *@

<div class="component-sitelistsubsiteslistnb">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>subsiteslist nb</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            subsiteslist nb component requires configuration.
        </div>
    }
</div>
