@model WCMS.WebSystem.WebParts.G2.ViewComponents.DownloadPmDownloadsEdit02ViewModel
@* Ported from PM_Downloads_Edit_02.ascx (AppBundle2/Download). *@

<div class="component-downloadpmdownloadsedit02">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>PM Downloads Edit 02</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            PM Downloads Edit 02 component requires configuration.
        </div>
    }
</div>
