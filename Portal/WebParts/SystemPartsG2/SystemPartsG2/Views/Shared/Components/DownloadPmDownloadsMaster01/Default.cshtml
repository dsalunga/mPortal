@model WCMS.WebSystem.WebParts.G2.ViewComponents.DownloadPmDownloadsMaster01ViewModel
@* Ported from PM_Downloads_Master_01.ascx (AppBundle2/Download). *@

<div class="component-downloadpmdownloadsmaster01">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>PM Downloads Master 01</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            PM Downloads Master 01 component requires configuration.
        </div>
    }
</div>
