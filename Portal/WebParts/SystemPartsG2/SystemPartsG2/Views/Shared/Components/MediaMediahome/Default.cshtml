@model WCMS.WebSystem.WebParts.G2.ViewComponents.MediaMediahomeViewModel
@* Ported from mediahome.ascx (AppBundle2/Media). *@

<div class="component-mediamediahome">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>mediahome</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            mediahome component requires configuration.
        </div>
    }
</div>
