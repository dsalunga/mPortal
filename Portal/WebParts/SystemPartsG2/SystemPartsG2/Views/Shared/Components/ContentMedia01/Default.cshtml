@model WCMS.WebSystem.WebParts.G2.ViewComponents.ContentMedia01ViewModel
@* Ported from content_media_01.ascx (AppBundle2/Media). *@

<div class="component-contentmedia01">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>content media 01</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            content media 01 component requires configuration.
        </div>
    }
</div>
