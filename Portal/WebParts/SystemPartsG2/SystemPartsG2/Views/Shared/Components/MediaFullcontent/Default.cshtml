@model WCMS.WebSystem.WebParts.G2.ViewComponents.MediaFullcontentViewModel
@* Ported from fullcontent.ascx (AppBundle2/Media). *@

<div class="component-mediafullcontent">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>fullcontent</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            fullcontent component requires configuration.
        </div>
    }
</div>
