@model WCMS.WebSystem.WebParts.G2.ViewComponents.SmDownloads03ViewModel
@* Ported from SM_Downloads_03.ascx (AppBundle2/Download). *@

<div class="component-smdownloads03">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>SM Downloads 03</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            SM Downloads 03 component requires configuration.
        </div>
    }
</div>
