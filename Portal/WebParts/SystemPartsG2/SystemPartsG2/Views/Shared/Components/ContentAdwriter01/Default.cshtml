@model WCMS.WebSystem.WebParts.G2.ViewComponents.ContentAdwriter01ViewModel
@* Ported from content_adwriter_01.ascx (AppBundle2/Ads). *@

<div class="component-contentadwriter01">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>content adwriter 01</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            content adwriter 01 component requires configuration.
        </div>
    }
</div>
