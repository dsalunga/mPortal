@model WCMS.WebSystem.WebParts.G2.ViewComponents.ControlsBasiclistViewModel
@* Ported from BasicList.ascx (AppBundle2/Download/Controls). *@

<div class="component-controlsbasiclist">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Basic List</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Basic List component requires configuration.
        </div>
    }
</div>
