@model WCMS.WebSystem.WebParts.G2.ViewComponents.SitelistListsimplehoriz1ViewModel
@* Ported from listsimplehoriz1.ascx (AppBundle2/SiteList). *@

<div class="component-sitelistlistsimplehoriz1">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>listsimplehoriz1</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            listsimplehoriz1 component requires configuration.
        </div>
    }
</div>
