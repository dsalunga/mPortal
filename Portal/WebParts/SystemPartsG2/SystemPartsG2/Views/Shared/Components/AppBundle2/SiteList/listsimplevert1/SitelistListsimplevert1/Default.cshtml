@model WCMS.WebSystem.WebParts.G2.ViewComponents.SitelistListsimplevert1ViewModel
@* Ported from listsimplevert1.ascx. *@

<div class="component-sitelist-listsimplevert1">
    @if (Model.Items.Any())
    {
        <div class="list-group">
            @foreach (var item in Model.Items)
            {
                <div class="list-group-item">
                    <span>@item.Name</span>
                </div>
            }
        </div>
    }
    else
    {
        <p class="text-muted fst-italic">No items available.</p>
    }
</div>