@model WCMS.WebSystem.WebParts.G2.ViewComponents.ControlsGroupbyyearViewModel
@* Ported from GroupByYear.ascx. *@

<div class="component-controls-groupbyyear">
    @if (Model.Items.Any())
    {
        <div class="list-group">
            @foreach (var item in Model.Items)
            {
                <div class="list-group-item">
                    <span class="me-3">@item.FileYear</span>
                </div>
            }
        </div>
    }
    else
    {
        <p class="text-muted fst-italic">No items available.</p>
    }
</div>