@model WCMS.WebSystem.WebParts.ViewComponents.WmCreatesurveypage04ViewModel
@* Ported from WM_CreateSurveyPage_04.ascx (SystemParts). *@

<div class="component-wmcreatesurveypage04">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>WM Create Survey Page 04</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> WM Create Survey Page 04 component requires configuration.
        </div>
    }
</div>
