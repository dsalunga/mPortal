@model WCMS.WebSystem.WebParts.ViewComponents.WmSurveyquestions05ViewModel
@* Ported from WM_SurveyQuestions_05.ascx (SystemParts). *@

<div class="component-wmsurveyquestions05">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>WM Survey Questions 05</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Generic List / Survey:</strong> WM Survey Questions 05 component requires configuration.
        </div>
    }
</div>
