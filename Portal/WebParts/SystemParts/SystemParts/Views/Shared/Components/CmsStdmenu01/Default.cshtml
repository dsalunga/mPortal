@model WCMS.WebSystem.WebParts.ViewComponents.CmsStdmenu01ViewModel
@* Ported from CMS_StdMenu_01.ascx (SystemParts). *@

<div class="component-cmsstdmenu01">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>CMS Std Menu 01</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>CMS:</strong> CMS Std Menu 01 component requires configuration.
        </div>
    }
</div>
