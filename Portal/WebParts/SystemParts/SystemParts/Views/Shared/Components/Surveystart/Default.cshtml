@model WCMS.WebSystem.WebParts.ViewComponents.SurveystartViewModel
@* Ported from SurveyStart.ascx (SystemParts). *@

<div class="component-surveystart">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Survey Start</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Generic List / Survey:</strong> Survey Start component requires configuration.
        </div>
    }
</div>
