@model WCMS.WebSystem.WebParts.ViewComponents.FileManagerBreadcrumbViewModel
@* Ported from Breadcrumb.ascx (SystemParts). *@

<div class="component-filemanagerbreadcrumb">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Breadcrumb</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> Breadcrumb component requires configuration.
        </div>
    }
</div>
