@model WCMS.WebSystem.WebParts.ViewComponents.IndexerbreadcrumbViewModel
@* Ported from IndexerBreadcrumb.ascx (SystemParts). *@

<div class="component-indexerbreadcrumb">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Indexer Breadcrumb</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> Indexer Breadcrumb component requires configuration.
        </div>
    }
</div>
