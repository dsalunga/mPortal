@model WCMS.WebSystem.WebParts.ViewComponents.AdmineventViewModel
@* Ported from AdminEvent.ascx (SystemParts). *@

<div class="component-adminevent">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Event</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Event component requires configuration.
        </div>
    }
</div>
