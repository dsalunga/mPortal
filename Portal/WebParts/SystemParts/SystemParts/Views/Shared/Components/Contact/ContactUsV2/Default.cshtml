@model WCMS.WebSystem.WebParts.ViewComponents.Contactusv2FormModel
@* Ported from ContactUsV2.ascx (Contact). reCAPTCHA-enabled contact form. *@

<section class="Contact-Us wcms-contact-v2" aria-label="Contact us">
    @if (Model.Submitted)
    {
        <div class="alert alert-success" role="status" aria-live="polite">
            <i class="bi bi-check-circle"></i> Thank you for your message. We will get back to you shortly.
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-body">
                <h5 class="card-title mb-3">Contact Us</h5>
                <form method="post" asp-action="Submit" asp-controller="ContentApi" aria-label="Contact form">
                    <input type="hidden" name="objectId" value="@Model.ObjectId" />
                    <input type="hidden" name="recordId" value="@Model.RecordId" />
                    @if (Model.IsLoggedIn)
                    {
                        <div class="mb-3">
                            <label class="form-label">Name</label>
                            <p class="form-control-plaintext fw-bold">@Model.Name</p>
                            <input type="hidden" name="name" value="@Model.Name" />
                        </div>
                    }
                    else
                    {
                        <div class="mb-3">
                            <label for="contactV2Name-@Model.ObjectId" class="form-label">
                                Name <span class="text-danger" aria-hidden="true">*</span>
                            </label>
                            <input type="text" id="contactV2Name-@Model.ObjectId" name="name"
                                   class="form-control" value="@Model.Name" required aria-required="true" />
                        </div>
                    }
                    @if (!Model.IsLoggedIn)
                    {
                        <div class="mb-3">
                            <label for="contactV2Email-@Model.ObjectId" class="form-label">
                                Email Address <span class="text-danger" aria-hidden="true">*</span>
                            </label>
                            <input type="email" id="contactV2Email-@Model.ObjectId" name="email"
                                   class="form-control" value="@Model.Email" required aria-required="true" />
                        </div>
                        <div class="mb-3">
                            <label for="contactV2Phone-@Model.ObjectId" class="form-label">Contact No</label>
                            <input type="tel" id="contactV2Phone-@Model.ObjectId" name="contactNo" class="form-control" />
                        </div>
                    }
                    <div class="mb-3">
                        <label for="contactV2Subject-@Model.ObjectId" class="form-label">
                            Subject <span class="text-danger" aria-hidden="true">*</span>
                        </label>
                        <input type="text" id="contactV2Subject-@Model.ObjectId" name="subject"
                               class="form-control" required aria-required="true" />
                    </div>
                    <div class="mb-3">
                        <label for="contactV2Message-@Model.ObjectId" class="form-label">
                            Message <span class="text-danger" aria-hidden="true">*</span>
                        </label>
                        <textarea id="contactV2Message-@Model.ObjectId" name="message"
                                  class="form-control" rows="6" required aria-required="true"></textarea>
                    </div>
                    @if (Model.RequireCode && !Model.IsLoggedIn && !string.IsNullOrEmpty(Model.RecaptchaSiteKey))
                    {
                        <div class="mb-3">
                            <label class="form-label">
                                Verification Code <span class="text-danger" aria-hidden="true">*</span>
                            </label>
                            <div class="g-recaptcha" data-sitekey="@Model.RecaptchaSiteKey"></div>
                        </div>
                    }
                    <div class="d-flex justify-content-between align-items-center">
                        <small class="text-muted fst-italic">
                            <span class="text-danger" aria-hidden="true">*</span> Required fields
                        </small>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-send"></i> Submit
                        </button>
                    </div>
                </form>
            </div>
        </div>
    }
</section>
