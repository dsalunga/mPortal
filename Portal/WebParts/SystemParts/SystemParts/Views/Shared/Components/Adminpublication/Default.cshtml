@model WCMS.WebSystem.WebParts.ViewComponents.AdminpublicationViewModel
@* Ported from AdminPublication.ascx (SystemParts). *@

<div class="component-adminpublication">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Publication</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Publication component requires configuration.
        </div>
    }
</div>
