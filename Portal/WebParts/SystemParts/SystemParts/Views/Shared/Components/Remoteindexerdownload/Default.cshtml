@model WCMS.WebSystem.WebParts.ViewComponents.RemoteindexerdownloadViewModel
@* Ported from RemoteIndexerDownload.ascx (SystemParts). *@

<div class="component-remoteindexerdownload">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Remote Indexer Download</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> Remote Indexer Download component requires configuration.
        </div>
    }
</div>
