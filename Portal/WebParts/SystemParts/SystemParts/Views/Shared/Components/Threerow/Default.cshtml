@model WCMS.WebSystem.WebParts.ViewComponents.ThreerowViewModel
@* Ported from ThreeRow.ascx (SystemParts). *@

<div class="component-threerow">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Three Row</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Three Row component requires configuration.
        </div>
    }
</div>
