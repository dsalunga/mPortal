@model WCMS.WebSystem.WebParts.ViewComponents.FullviewViewModel
@* Ported from FullView.ascx (SystemParts). *@

<div class="component-fullview">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Full View</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Photo:</strong> Full View component requires configuration.
        </div>
    }
</div>
