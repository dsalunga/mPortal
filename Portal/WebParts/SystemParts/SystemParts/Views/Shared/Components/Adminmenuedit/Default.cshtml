@model WCMS.WebSystem.WebParts.ViewComponents.AdminmenueditViewModel
@* Ported from AdminMenuEdit.ascx (SystemParts). *@

<div class="component-adminmenuedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Menu Edit</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Menu Edit component requires configuration.
        </div>
    }
</div>
