@model WCMS.WebSystem.WebParts.ViewComponents.ConfigpublicationViewModel
@* Ported from ConfigPublication.ascx (SystemParts). *@

<div class="component-configpublication">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Publication</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Publication component requires configuration.
        </div>
    }
</div>
