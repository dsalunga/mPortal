@model WCMS.WebSystem.WebParts.ViewComponents.Configdynamicmenuv2ViewModel
@* Ported from ConfigDynamicMenuV2.ascx (SystemParts). *@

<div class="component-configdynamicmenuv2">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Dynamic Menu V2</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Dynamic Menu V2 component requires configuration.
        </div>
    }
</div>
