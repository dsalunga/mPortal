@model WCMS.WebSystem.WebParts.ViewComponents.AdmininquiriesdetailsViewModel
@* Ported from AdminInquiriesDetails.ascx (SystemParts). *@

<div class="component-admininquiriesdetails">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Inquiries Details</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Inquiries Details component requires configuration.
        </div>
    }
</div>
