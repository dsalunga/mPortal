@model WCMS.WebSystem.WebParts.ViewComponents.ConfigsendemailViewModel
@* Ported from ConfigSendEmail.ascx (SystemParts). *@

<div class="component-configsendemail">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Send Email</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Send Email component requires configuration.
        </div>
    }
</div>
