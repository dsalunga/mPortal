@model WCMS.WebSystem.WebParts.ViewComponents.WmSurveypages03ViewModel
@* Ported from WM_SurveyPages_03.ascx (SystemParts). *@

<div class="component-wmsurveypages03">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>WM Survey Pages 03</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Generic List / Survey:</strong> WM Survey Pages 03 component requires configuration.
        </div>
    }
</div>
