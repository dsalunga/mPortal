@model WCMS.WebSystem.WebParts.ViewComponents.AdminremotelibraryeditViewModel
@* Ported from AdminRemoteLibraryEdit.ascx (SystemParts). *@

<div class="component-adminremotelibraryedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Remote Library Edit</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Remote Library Edit component requires configuration.
        </div>
    }
</div>
