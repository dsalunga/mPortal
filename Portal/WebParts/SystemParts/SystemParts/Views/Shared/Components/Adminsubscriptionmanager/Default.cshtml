@model WCMS.WebSystem.WebParts.ViewComponents.AdminsubscriptionmanagerViewModel
@* Ported from AdminSubscriptionManager.ascx (SystemParts). *@

<div class="component-adminsubscriptionmanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Subscription Manager</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Subscription Manager component requires configuration.
        </div>
    }
</div>
