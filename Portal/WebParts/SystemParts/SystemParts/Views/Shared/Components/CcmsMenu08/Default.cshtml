@model WCMS.WebSystem.WebParts.ViewComponents.CcmsMenu08ViewModel
@* Ported from CCMS_Menu_08.ascx (SystemParts). *@

<div class="component-ccmsmenu08">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>CCMS Menu 08</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Menu:</strong> CCMS Menu 08 component requires configuration.
        </div>
    }
</div>
