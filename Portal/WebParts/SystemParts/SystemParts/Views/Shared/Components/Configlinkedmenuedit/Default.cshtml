@model WCMS.WebSystem.WebParts.ViewComponents.ConfiglinkedmenueditViewModel
@* Ported from ConfigLinkedMenuEdit.ascx (SystemParts). *@

<div class="component-configlinkedmenuedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Linked Menu Edit</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Linked Menu Edit component requires configuration.
        </div>
    }
</div>
