@model WCMS.WebSystem.WebParts.ViewComponents.CcmsGallery02ViewModel
@* Ported from CCMS_Gallery_02.ascx (SystemParts). *@

<div class="component-ccmsgallery02">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>CCMS Gallery 02</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Photo:</strong> CCMS Gallery 02 component requires configuration.
        </div>
    }
</div>
