@model WCMS.WebSystem.WebParts.ViewComponents.AdminalbumViewModel
@* Ported from AdminAlbum.ascx (SystemParts). *@

<div class="component-adminalbum">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Album</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Album component requires configuration.
        </div>
    }
</div>
