@model WCMS.WebSystem.WebParts.ViewComponents.AdminalbumeditViewModel
@* Ported from AdminAlbumEdit.ascx (SystemParts). *@

<div class="component-adminalbumedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Album Edit</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Album Edit component requires configuration.
        </div>
    }
</div>
