@model WCMS.WebSystem.WebParts.ViewComponents.AdmincontactdetailsViewModel
@* Ported from AdminContactDetails.ascx (SystemParts). *@

<div class="component-admincontactdetails">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Contact Details</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Contact Details component requires configuration.
        </div>
    }
</div>
