@model WCMS.WebSystem.WebParts.ViewComponents.AdminremotelibrarymanagerViewModel
@* Ported from AdminRemoteLibraryManager.ascx (SystemParts). *@

<div class="component-adminremotelibrarymanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Remote Library Manager</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Remote Library Manager component requires configuration.
        </div>
    }
</div>
