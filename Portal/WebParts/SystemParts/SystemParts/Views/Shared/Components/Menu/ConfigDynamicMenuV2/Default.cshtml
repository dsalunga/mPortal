@model WCMS.WebSystem.WebParts.ViewComponents.Configdynamicmenuv2ViewModel
@*
    Ported from legacy/Portal/WebParts/SystemParts/SystemParts/AppBundle/Menu/ConfigDynamicMenuV2.ascx.
    Real Razor admin shell generated from legacy WebForms markup. The form fields,
    list, and submit buttons reflect the legacy control structure. Persistence is
    a TODO and must be wired to the appropriate controller action.
*@
<section class="card shadow-sm component-configdynamicmenuv2" aria-label="Config Dynamic Menu V2">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-pencil-square" aria-hidden="true"></i> Config Dynamic Menu V2</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.StatusMessage))
        {
            <div class="alert alert-success" role="status">@Model.StatusMessage</div>
        }
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger" role="alert">@Model.ErrorMessage</div>
        }

        <form method="post" asp-action="@Model.PostAction" asp-controller="@Model.PostController" enctype="multipart/form-data" novalidate>
            <input type="hidden" name="objectId" value="@Model.ObjectId" />
            <input type="hidden" name="recordId" value="@Model.RecordId" />

                <div class="mb-3">
                    <label class="form-label" for="f-cbomenu">Cbo Menu</label>
                    <select class="form-select" id="f-cbomenu" name="cboMenu">
                        <option value="">-- Select --</option>
                        @* TODO: populate options from service *@
                    </select>
                </div>
                <div class="mb-3">
                    <strong>Status:</strong>
                    <span>@Model.lblStatus</span>
                </div>
            <div class="mt-3">
                <button type="submit" class="btn btn-primary me-2" name="action" value="Save">Save</button>
                <a class="btn btn-outline-secondary" href="javascript:history.back()">Cancel</a>
            </div>
        </form>
    </div>
</section>
