@model WCMS.WebSystem.WebParts.ViewComponents.ConfigcontentViewModel
@* Ported from ConfigContent.ascx (SystemParts). *@

<div class="component-configcontent">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Content</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Content component requires configuration.
        </div>
    }
</div>
