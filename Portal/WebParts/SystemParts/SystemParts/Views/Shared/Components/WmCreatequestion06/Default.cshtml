@model WCMS.WebSystem.WebParts.ViewComponents.WmCreatequestion06ViewModel
@* Ported from WM_CreateQuestion_06.ascx (SystemParts). *@

<div class="component-wmcreatequestion06">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>WM Create Question 06</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> WM Create Question 06 component requires configuration.
        </div>
    }
</div>
