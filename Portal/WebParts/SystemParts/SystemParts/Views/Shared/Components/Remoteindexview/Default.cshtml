@model WCMS.WebSystem.WebParts.ViewComponents.RemoteindexviewViewModel
@* Ported from RemoteIndexView.ascx (SystemParts). *@

<div class="component-remoteindexview">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Remote Index View</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> Remote Index View component requires configuration.
        </div>
    }
</div>
