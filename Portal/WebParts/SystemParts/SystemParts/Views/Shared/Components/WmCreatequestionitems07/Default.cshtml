@model WCMS.WebSystem.WebParts.ViewComponents.WmCreatequestionitems07ViewModel
@* Ported from WM_CreateQuestionItems_07.ascx (SystemParts). *@

<div class="component-wmcreatequestionitems07">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>WM Create Question Items 07</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> WM Create Question Items 07 component requires configuration.
        </div>
    }
</div>
