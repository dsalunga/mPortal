@model WCMS.WebSystem.WebParts.ViewComponents.ArticleMultipleMultiNewsroomViewModel
@* Ported from Newsroom.ascx (Article/Templates/Multiple). *@

<div class="article-template article-multi-newsroom mb-3" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
    @Html.Raw(Model.ImageHtml)
    <div class="fw-bold">
        @Html.Raw(Model.TitleHtml)
    </div>
    <div class="text-muted small">
        @Html.Raw(Model.DateHtml)
    </div>
    <div>
        @Html.Raw(Model.DescriptionHtml)
    </div>
    <div class="text-end">
        @Html.Raw(Model.LinkHtml)
    </div>
    <hr class="my-2" />
</div>