@model WCMS.WebSystem.WebParts.ViewComponents.WmResults08ViewModel
@* Ported from WM_Results_08.ascx (SystemParts). *@

<div class="component-wmresults08">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>WM Results 08</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Generic List / Survey:</strong> WM Results 08 component requires configuration.
        </div>
    }
</div>
