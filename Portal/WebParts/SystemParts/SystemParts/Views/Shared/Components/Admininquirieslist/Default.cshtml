@model WCMS.WebSystem.WebParts.ViewComponents.AdmininquirieslistViewModel
@* Ported from AdminInquiriesList.ascx (SystemParts). *@

<div class="component-admininquirieslist">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Inquiries List</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Inquiries List component requires configuration.
        </div>
    }
</div>
