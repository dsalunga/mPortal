@model WCMS.WebSystem.WebParts.ViewComponents.SurveyfinishViewModel
@* Ported from SurveyFinish.ascx (SystemParts). *@

<div class="component-surveyfinish">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Survey Finish</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Generic List / Survey:</strong> Survey Finish component requires configuration.
        </div>
    }
</div>
