@model WCMS.WebSystem.WebParts.ViewComponents.AdmincalendarmanagerViewModel
@* Ported from AdminCalendarManager.ascx (SystemParts). *@

<div class="component-admincalendarmanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Calendar Manager</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Calendar Manager component requires configuration.
        </div>
    }
</div>
