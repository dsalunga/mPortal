@model WCMS.WebSystem.WebParts.ViewComponents.Configdynamicmenu01ViewModel
@* Ported from ConfigDynamicMenu01.ascx (SystemParts). *@

<div class="component-configdynamicmenu01">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Dynamic Menu01</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Dynamic Menu01 component requires configuration.
        </div>
    }
</div>
