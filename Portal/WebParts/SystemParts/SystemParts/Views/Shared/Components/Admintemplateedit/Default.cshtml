@model WCMS.WebSystem.WebParts.ViewComponents.AdmintemplateeditViewModel
@* Ported from AdminTemplateEdit.ascx (SystemParts). *@

<div class="component-admintemplateedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Template Edit</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Template Edit component requires configuration.
        </div>
    }
</div>
