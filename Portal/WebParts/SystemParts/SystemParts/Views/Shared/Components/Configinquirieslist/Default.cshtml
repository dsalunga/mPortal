@model WCMS.WebSystem.WebParts.ViewComponents.ConfiginquirieslistViewModel
@* Ported from ConfigInquiriesList.ascx (SystemParts). *@

<div class="component-configinquirieslist">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Inquiries List</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Inquiries List component requires configuration.
        </div>
    }
</div>
