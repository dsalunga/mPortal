@model WCMS.WebSystem.WebParts.ViewComponents.ConfiglinkedmenuViewModel
@* Ported from ConfigLinkedMenu.ascx (SystemParts). *@

<div class="component-configlinkedmenu">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Linked Menu</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Linked Menu component requires configuration.
        </div>
    }
</div>
