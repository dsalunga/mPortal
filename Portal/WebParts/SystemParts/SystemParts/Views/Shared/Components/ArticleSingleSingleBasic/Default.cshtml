@model WCMS.WebSystem.WebParts.ViewComponents.ArticleSingleSingleBasicViewModel
@* Ported from Basic.ascx (SystemParts). *@

<div class="component-articlesinglesinglebasic">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Basic</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Article:</strong> Basic component requires configuration.
        </div>
    }
</div>
