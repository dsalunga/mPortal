@model WCMS.WebSystem.WebParts.ViewComponents.ConfigfilemanagerViewModel
@* Ported from ConfigFileManager.ascx (SystemParts). *@

<div class="component-configfilemanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config File Manager</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config File Manager component requires configuration.
        </div>
    }
</div>
