@model WCMS.WebSystem.WebParts.ViewComponents.AdmintemplatecomposerViewModel
@* Ported from AdminTemplateComposer.ascx (SystemParts). *@

<div class="component-admintemplatecomposer">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Template Composer</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Template Composer component requires configuration.
        </div>
    }
</div>
