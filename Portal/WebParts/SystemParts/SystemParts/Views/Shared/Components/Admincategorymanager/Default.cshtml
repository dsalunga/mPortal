@model WCMS.WebSystem.WebParts.ViewComponents.AdmincategorymanagerViewModel
@* Ported from AdminCategoryManager.ascx (SystemParts). *@

<div class="component-admincategorymanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Category Manager</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Category Manager component requires configuration.
        </div>
    }
</div>
