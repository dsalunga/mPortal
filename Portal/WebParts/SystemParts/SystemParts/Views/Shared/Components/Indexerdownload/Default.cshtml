@model WCMS.WebSystem.WebParts.ViewComponents.IndexerdownloadViewModel
@* Ported from IndexerDownload.ascx (SystemParts). *@

<div class="component-indexerdownload">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Indexer Download</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>File Manager:</strong> Indexer Download component requires configuration.
        </div>
    }
</div>
