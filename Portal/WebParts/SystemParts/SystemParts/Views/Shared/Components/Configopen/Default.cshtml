@model WCMS.WebSystem.WebParts.ViewComponents.ConfigopenViewModel
@* Ported from ConfigOpen.ascx (SystemParts). *@

<div class="component-configopen">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Open</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>Configuration:</strong> Config Open component requires configuration.
        </div>
    }
</div>
