@model WCMS.WebSystem.WebParts.ViewComponents.AdmintemplatemanagerViewModel
@* Ported from AdminTemplateManager.ascx (SystemParts). *@

<div class="component-admintemplatemanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Template Manager</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Template Manager component requires configuration.
        </div>
    }
</div>
