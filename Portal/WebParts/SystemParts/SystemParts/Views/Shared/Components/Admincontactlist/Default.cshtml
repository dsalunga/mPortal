@model WCMS.WebSystem.WebParts.ViewComponents.AdmincontactlistViewModel
@* Ported from AdminContactList.ascx (SystemParts). *@

<div class="component-admincontactlist">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Contact List</em> — ported from legacy WebForms. 
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            <strong>System:</strong> Admin Contact List component requires configuration.
        </div>
    }
</div>
