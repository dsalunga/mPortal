@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ProfileUpdatemygroupsViewModel
@* Ported from UpdateMyGroups.ascx (Themes/area51/Parts/Profile/Profile). *@

<div class="component-profileupdatemygroups">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Update My Groups</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Update My Groups component requires configuration.
        </div>
    }
</div>
