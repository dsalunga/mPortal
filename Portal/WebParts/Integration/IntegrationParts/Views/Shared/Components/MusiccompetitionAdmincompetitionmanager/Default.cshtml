@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MusiccompetitionAdmincompetitionmanagerViewModel
@* Ported from AdminCompetitionManager.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-musiccompetitionadmincompetitionmanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Competition Manager</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Admin Competition Manager component requires configuration.
        </div>
    }
</div>
