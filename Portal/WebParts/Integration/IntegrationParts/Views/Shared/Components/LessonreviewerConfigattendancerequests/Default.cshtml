@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LessonreviewerConfigattendancerequestsViewModel
@* Ported from ConfigAttendanceRequests.ascx (Apps/Integration/Profile/LessonReviewer). *@

<div class="component-lessonreviewerconfigattendancerequests">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Attendance Requests</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Config Attendance Requests component requires configuration.
        </div>
    }
</div>
