@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ProfileMypreferencesViewModel
@* Ported from MyPreferences.ascx (Themes/area51/Parts/Profile/Profile). *@

<div class="component-profilemypreferences">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>My Preferences</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            My Preferences component requires configuration.
        </div>
    }
</div>
