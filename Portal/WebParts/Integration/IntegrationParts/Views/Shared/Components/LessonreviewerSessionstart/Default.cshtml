@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LessonreviewerSessionstartViewModel
@* Ported from SessionStart.ascx (Apps/Integration/Profile/LessonReviewer). *@

<div class="component-lessonreviewersessionstart">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Session Start</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Session Start component requires configuration.
        </div>
    }
</div>
