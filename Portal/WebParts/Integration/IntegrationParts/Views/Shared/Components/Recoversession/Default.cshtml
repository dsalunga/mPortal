@model WCMS.WebSystem.WebParts.Integration.ViewComponents.RecoversessionViewModel
@* Ported from RecoverSession.ascx (Apps/Integration/Profile/LessonReviewer). *@

<div class="component-recoversession">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Recover Session</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Recover Session component requires configuration.
        </div>
    }
</div>
