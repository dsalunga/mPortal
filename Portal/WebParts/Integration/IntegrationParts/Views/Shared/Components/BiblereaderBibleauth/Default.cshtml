@model WCMS.WebSystem.WebParts.Integration.ViewComponents.BiblereaderBibleauthViewModel
@* Ported from BibleAuth.ascx (Apps/Integration/BibleReader). *@

<div class="component-biblereaderbibleauth">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Bible Auth</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Bible Auth component requires configuration.
        </div>
    }
</div>
