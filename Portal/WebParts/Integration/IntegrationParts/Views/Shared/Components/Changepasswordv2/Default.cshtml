@model WCMS.WebSystem.WebParts.Integration.ViewComponents.Changepasswordv2ViewModel
@* Ported from ChangePasswordV2.ascx (Apps/Integration/Account). *@

<div class="component-changepasswordv2">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Change Password V2</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Change Password V2 component requires configuration.
        </div>
    }
</div>
