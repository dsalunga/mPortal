@model WCMS.WebSystem.WebParts.ViewComponents.IntMyAttendanceModel

<div class="card" id="int-my-attendance-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">My Attendance</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>My attendance records placeholder content.</p>
    </div>
</div>
