@model WCMS.WebSystem.WebParts.ViewComponents.IntAccessRequestModel

<div class="card" id="int-access-request-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Access Request</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Access request form placeholder content.</p>
    </div>
</div>
