@model WCMS.WebSystem.WebParts.Integration.ViewComponents.Area51IndexViewModel
@* Ported from index.ascx (Themes/area51). *@

<div class="component-area51index">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>index</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            index component requires configuration.
        </div>
    }
</div>
