@model WCMS.WebSystem.WebParts.ViewComponents.IntSportsfestManagerModel

<div class="card" id="int-sportsfest-manager-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Sportsfest Manager</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Sportsfest management placeholder content.</p>
    </div>
</div>
