@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LessonreviewerAttendancerequestsViewModel
@* Ported from AttendanceRequests.ascx (Apps/Integration/LessonReviewer). *@

<div class="component-lessonreviewerattendancerequests">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Attendance Requests</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Attendance Requests component requires configuration.
        </div>
    }
</div>
