@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MusiccompetitionMessageboardv2ViewModel
@* Ported from MessageBoardV2.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-musiccompetitionmessageboardv2">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Message Board V2</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Message Board V2 component requires configuration.
        </div>
    }
</div>
