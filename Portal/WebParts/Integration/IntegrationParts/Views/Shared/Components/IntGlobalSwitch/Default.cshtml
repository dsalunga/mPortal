@model WCMS.WebSystem.WebParts.ViewComponents.IntGlobalSwitchModel

<div class="card" id="int-global-switch-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Global Switch</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Global switch control placeholder content.</p>
    </div>
</div>
