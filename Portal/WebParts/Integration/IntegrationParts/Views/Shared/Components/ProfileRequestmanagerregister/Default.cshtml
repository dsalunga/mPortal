@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ProfileRequestmanagerregisterViewModel
@* Ported from RequestManagerRegister.ascx (Apps/Integration/Profile). *@

<div class="component-profilerequestmanagerregister">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Request Manager Register</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Request Manager Register component requires configuration.
        </div>
    }
</div>
