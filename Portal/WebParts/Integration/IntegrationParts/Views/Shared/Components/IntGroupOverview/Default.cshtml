@model WCMS.WebSystem.WebParts.ViewComponents.IntGroupOverviewModel

<div class="card" id="int-group-overview-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Group Overview</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Group overview placeholder content.</p>
    </div>
</div>
