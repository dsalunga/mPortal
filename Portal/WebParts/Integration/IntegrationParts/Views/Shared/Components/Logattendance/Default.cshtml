@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LogattendanceViewModel
@* Ported from LogAttendance.ascx (Apps/Integration/Profile/LessonReviewer). *@

<div class="component-logattendance">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Log Attendance</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Log Attendance component requires configuration.
        </div>
    }
</div>
