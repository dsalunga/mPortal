@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LessonreviewerConfigattendancerequestViewModel
@* Ported from ConfigAttendanceRequest.ascx (Apps/Integration/Profile/LessonReviewer). *@

<div class="component-lessonreviewerconfigattendancerequest">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Attendance Request</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Config Attendance Request component requires configuration.
        </div>
    }
</div>
