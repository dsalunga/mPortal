@model WCMS.WebSystem.WebParts.Integration.ViewComponents.AccountRegisterv2ViewModel
@* Ported from RegisterV2.ascx (Apps/Integration/Account). *@

<div class="component-accountregisterv2">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Register V2</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Register V2 component requires configuration.
        </div>
    }
</div>
