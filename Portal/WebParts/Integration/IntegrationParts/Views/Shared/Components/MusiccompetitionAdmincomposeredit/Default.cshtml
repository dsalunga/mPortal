@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MusiccompetitionAdmincomposereditViewModel
@* Ported from AdminComposerEdit.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-musiccompetitionadmincomposeredit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Composer Edit</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Admin Composer Edit component requires configuration.
        </div>
    }
</div>
