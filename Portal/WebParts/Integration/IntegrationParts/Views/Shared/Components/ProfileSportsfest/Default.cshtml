@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ProfileSportsfestViewModel
@* Ported from Sportsfest.ascx (Apps/Integration/Profile). *@

<div class="component-profilesportsfest">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Sportsfest</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Sportsfest component requires configuration.
        </div>
    }
</div>
