@model WCMS.WebSystem.WebParts.Integration.ViewComponents.Area51Area51DefaultViewModel
@* Ported from Default.ascx (Themes/area51). *@

<div class="component-area51area51default">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Default</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Default component requires configuration.
        </div>
    }
</div>
