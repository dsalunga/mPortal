@model WCMS.WebSystem.WebParts.ViewComponents.IntLogAttendanceModel

<div class="card" id="int-log-attendance-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Log Attendance</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Log attendance placeholder content.</p>
    </div>
</div>
