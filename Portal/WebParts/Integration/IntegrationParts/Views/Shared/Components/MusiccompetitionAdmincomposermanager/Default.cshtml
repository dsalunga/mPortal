@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MusiccompetitionAdmincomposermanagerViewModel
@* Ported from AdminComposerManager.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-musiccompetitionadmincomposermanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Composer Manager</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Admin Composer Manager component requires configuration.
        </div>
    }
</div>
