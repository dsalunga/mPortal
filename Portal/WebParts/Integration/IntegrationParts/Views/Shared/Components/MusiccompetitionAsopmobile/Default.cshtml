@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MusiccompetitionAsopmobileViewModel
@* Ported from ASOPMobile.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-musiccompetitionasopmobile">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>ASOPMobile</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            ASOPMobile component requires configuration.
        </div>
    }
</div>
