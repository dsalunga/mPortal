@model WCMS.WebSystem.WebParts.ViewComponents.IntMemberSessionsModel

<div class="card" id="int-member-sessions-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Member Sessions</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Member sessions placeholder content.</p>
    </div>
</div>
