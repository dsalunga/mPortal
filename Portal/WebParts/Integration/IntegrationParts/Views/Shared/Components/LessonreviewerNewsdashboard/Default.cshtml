@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LessonreviewerNewsdashboardViewModel
@* Ported from NewsDashboard.ascx (Apps/Integration/Profile/LessonReviewer). *@

<div class="component-lessonreviewernewsdashboard">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>News Dashboard</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            News Dashboard component requires configuration.
        </div>
    }
</div>
