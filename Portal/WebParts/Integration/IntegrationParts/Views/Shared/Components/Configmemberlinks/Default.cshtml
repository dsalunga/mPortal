@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ConfigmemberlinksViewModel
@* Ported from ConfigMemberLinks.ascx (Apps/Integration/Registration). *@

<div class="component-configmemberlinks">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Member Links</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Config Member Links component requires configuration.
        </div>
    }
</div>
