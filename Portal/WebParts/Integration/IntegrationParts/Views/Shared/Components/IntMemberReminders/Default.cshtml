@model WCMS.WebSystem.WebParts.ViewComponents.IntMemberRemindersModel

<div class="card" id="int-member-reminders-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Member Reminders</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Member reminders placeholder content.</p>
    </div>
</div>
