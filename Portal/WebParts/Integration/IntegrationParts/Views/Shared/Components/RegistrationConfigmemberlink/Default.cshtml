@model WCMS.WebSystem.WebParts.Integration.ViewComponents.RegistrationConfigmemberlinkViewModel
@* Ported from ConfigMemberLink.ascx (Apps/Integration/Registration). *@

<div class="component-registrationconfigmemberlink">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config Member Link</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Config Member Link component requires configuration.
        </div>
    }
</div>
