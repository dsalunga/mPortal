@model WCMS.WebSystem.WebParts.ViewComponents.IntMemberEditModel

<div class="card" id="int-member-edit-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Edit Member</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Edit member placeholder content.</p>
    </div>
</div>
