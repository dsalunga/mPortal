@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ForgotpasswordViewModel
@* Ported from ForgotPassword.ascx (Apps/Integration/Profile). *@

<div class="component-forgotpassword">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Forgot Password</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Forgot Password component requires configuration.
        </div>
    }
</div>
