@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ProfileSportsfestmanagerViewModel
@* Ported from SportsfestManager.ascx. *@

<div class="component-profile-sportsfestmanager">
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="table-primary">
                <tr>
                    <th scope="col" style="width:60px" class="text-center">Actions</th>
                    <th scope="col">Name</th>
                    <th scope="col">Color</th>
                    <th scope="col">T-Shirt</th>
                    <th scope="col">Age</th>
                    <th scope="col">Mobile</th>
                    <th scope="col">Country</th>
                    <th scope="col">Locale</th>
                    <th scope="col">Suggestion</th>
                    <th scope="col">Date</th>
                    <th scope="col">Sports</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Items.Any())
                {
                    @foreach (var item in Model.Items)
                    {
                        <tr>
                            <td class="text-center">
                                <a href="?action=edit&id=@item.Id" class="btn btn-sm btn-outline-primary" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </a>
                            </td>
                            <td>@item.Name</td>
                            <td>@item.GroupColor</td>
                            <td>@item.ShirtSize</td>
                            <td>@item.Age</td>
                            <td>@item.Mobile</td>
                            <td>@item.Country</td>
                            <td>@item.Locale</td>
                            <td>@item.Suggestion</td>
                            <td>@item.EntryDate</td>
                            <td>@item.Sports</td>
                            <td>@item.Name</td>
                        </tr>
                    }
                }
                else
                {
                    <tr><td colspan="12" class="text-center text-muted fst-italic">No records found.</td></tr>
                }
            </tbody>
        </table>
    </div>

    @if (Model.TotalItems > Model.PageSize)
    {
        var pageCount = (int)Math.Ceiling((double)Model.TotalItems / Model.PageSize);
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center mt-3">
                <li class="page-item @(Model.CurrentPage <= 1 ? "disabled" : "")">
                    <a class="page-link" href="?page=@(Model.CurrentPage - 1)" aria-label="Previous">&laquo;</a>
                </li>
                @for (int i = 1; i <= pageCount; i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" href="?page=@i">@i</a>
                    </li>
                }
                <li class="page-item @(Model.CurrentPage >= pageCount ? "disabled" : "")">
                    <a class="page-link" href="?page=@(Model.CurrentPage + 1)" aria-label="Next">&raquo;</a>
                </li>
            </ul>
        </nav>
    }
</div>