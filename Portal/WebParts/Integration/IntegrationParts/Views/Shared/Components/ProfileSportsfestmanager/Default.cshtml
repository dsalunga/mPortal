@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ProfileSportsfestmanagerViewModel
@* Ported from SportsfestManager.ascx (Apps/Integration/Profile). *@

<div class="component-profilesportsfestmanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Sportsfest Manager</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Sportsfest Manager component requires configuration.
        </div>
    }
</div>
