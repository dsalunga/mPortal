@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MemberremindersViewModel
@* Ported from MemberReminders.ascx (Apps/Integration/Reminder). *@

<div class="component-memberreminders">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Member Reminders</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Member Reminders component requires configuration.
        </div>
    }
</div>
