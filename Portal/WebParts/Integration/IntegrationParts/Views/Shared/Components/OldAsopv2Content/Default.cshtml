@model WCMS.WebSystem.WebParts.Integration.ViewComponents.OldAsopv2ContentViewModel
@* Ported from asopv2-content.ascx (Themes/ASOP/v2/old). *@

<div class="component-oldasopv2content">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>asopv2-content</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            asopv2-content component requires configuration.
        </div>
    }
</div>
