@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LessonreviewerAttendancerequestViewModel
@* Ported from AttendanceRequest.ascx (Apps/Integration/LessonReviewer). *@

<div class="component-lessonreviewerattendancerequest">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Attendance Request</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Attendance Request component requires configuration.
        </div>
    }
</div>
