@model WCMS.WebSystem.WebParts.ViewComponents.IntReminderTemplatesModel

<div class="card" id="int-reminder-templates-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Reminder Templates</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Reminder templates placeholder content.</p>
    </div>
</div>
