@model WCMS.WebSystem.WebParts.ViewComponents.IntGroupEditModel

<div class="card" id="int-group-edit-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Edit Group</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Edit group placeholder content.</p>
    </div>
</div>
