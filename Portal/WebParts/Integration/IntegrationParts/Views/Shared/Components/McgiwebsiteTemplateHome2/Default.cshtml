@model WCMS.WebSystem.WebParts.Integration.ViewComponents.McgiwebsiteTemplateHome2ViewModel
@* Ported from template_home2.ascx (Themes/mcgi_website). *@

<div class="component-mcgiwebsitetemplatehome2">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>template home2</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            template home2 component requires configuration.
        </div>
    }
</div>
