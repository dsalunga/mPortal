@model WCMS.WebSystem.WebParts.ViewComponents.IntStreamingUserManagerModel

<div class="card" id="int-streaming-user-manager-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Streaming User Manager</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Streaming user management placeholder content.</p>
    </div>
</div>
