@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MusiccompetitionMessageboardViewModel
@* Ported from MessageBoard.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-musiccompetitionmessageboard">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Message Board</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Message Board component requires configuration.
        </div>
    }
</div>
