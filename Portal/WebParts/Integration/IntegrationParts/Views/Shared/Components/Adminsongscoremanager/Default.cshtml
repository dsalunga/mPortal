@model WCMS.WebSystem.WebParts.Integration.ViewComponents.AdminsongscoremanagerViewModel
@* Ported from AdminSongScoreManager.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-adminsongscoremanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Song Score Manager</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Admin Song Score Manager component requires configuration.
        </div>
    }
</div>
