@model WCMS.WebSystem.WebParts.ViewComponents.IntAttendanceRequestsModel

<div class="card" id="int-attendance-requests-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Attendance Requests</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Attendance requests list placeholder content.</p>
    </div>
</div>
