@model WCMS.WebSystem.WebParts.Integration.ViewComponents.AdmincompetitioneditViewModel
@* Ported from AdminCompetitionEdit.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-admincompetitionedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Admin Competition Edit</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Admin Competition Edit component requires configuration.
        </div>
    }
</div>
