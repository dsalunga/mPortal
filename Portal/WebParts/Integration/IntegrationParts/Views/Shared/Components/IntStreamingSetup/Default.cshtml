@model WCMS.WebSystem.WebParts.ViewComponents.IntStreamingSetupModel

<section aria-labelledby="streaming-setup-heading-@Model.ObjectId">
    <div class="card" id="int-streaming-setup-@Model.ObjectId">
        <div class="card-header">
            <h5 class="card-title mb-0" id="streaming-setup-heading-@Model.ObjectId">
                <i class="bi bi-gear" aria-hidden="true"></i> Streaming Setup
            </h5>
        </div>
        <div class="card-body">
            @if (!string.IsNullOrEmpty(Model.Message))
            {
                <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger") alert-dismissible fade show" role="status" aria-live="polite">
                    @Model.Message
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            <div id="streamingSetupContent-@Model.ObjectId" role="region" aria-label="Streaming configuration">
                <p class="text-muted"><i class="bi bi-info-circle" aria-hidden="true"></i> Streaming setup configuration will appear here.</p>
            </div>
        </div>
    </div>
</section>
