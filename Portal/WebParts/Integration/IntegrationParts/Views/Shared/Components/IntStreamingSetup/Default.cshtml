@model WCMS.WebSystem.WebParts.ViewComponents.IntStreamingSetupModel

<div class="card" id="int-streaming-setup-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Streaming Setup</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Streaming setup placeholder content.</p>
    </div>
</div>
