@model WCMS.WebSystem.WebParts.Integration.ViewComponents.CandidateeditViewModel
@* Ported from CandidateEdit.ascx (Apps/Integration/MusicCompetition). *@

<div class="component-candidateedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Candidate Edit</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Candidate Edit component requires configuration.
        </div>
    }
</div>
