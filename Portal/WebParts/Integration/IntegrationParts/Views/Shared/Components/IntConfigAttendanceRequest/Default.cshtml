@model WCMS.WebSystem.WebParts.ViewComponents.IntConfigAttendanceRequestModel

<div class="card" id="int-config-attendance-request-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Configure Attendance Request</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Config attendance request placeholder content.</p>
    </div>
</div>
