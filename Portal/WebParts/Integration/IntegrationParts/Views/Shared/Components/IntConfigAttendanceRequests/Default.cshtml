@model WCMS.WebSystem.WebParts.ViewComponents.IntConfigAttendanceRequestsModel

<div class="card" id="int-config-attendance-requests-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Attendance Requests</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Config attendance requests placeholder content.</p>
    </div>
</div>
