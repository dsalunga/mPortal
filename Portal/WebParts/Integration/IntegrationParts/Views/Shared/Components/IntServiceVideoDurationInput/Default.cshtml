@model WCMS.WebSystem.WebParts.ViewComponents.IntServiceVideoDurationInputModel

<div class="card" id="int-service-video-duration-input-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Service Video Duration</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Service video duration input placeholder content.</p>
    </div>
</div>
