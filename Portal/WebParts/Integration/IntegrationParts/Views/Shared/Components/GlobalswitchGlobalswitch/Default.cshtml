@model WCMS.WebSystem.WebParts.Integration.ViewComponents.GlobalswitchGlobalswitchViewModel
@* Ported from GlobalSwitch.ascx (Apps/Integration/GlobalSwitch). *@

<div class="component-globalswitchglobalswitch">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Global Switch</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Global Switch component requires configuration.
        </div>
    }
</div>
