@model WCMS.WebSystem.WebParts.Integration.ViewComponents.MembereditViewModel
@* Ported from MemberEdit.ascx (Apps/Integration/MasterList). *@

<div class="component-memberedit">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Member Edit</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Member Edit component requires configuration.
        </div>
    }
</div>
