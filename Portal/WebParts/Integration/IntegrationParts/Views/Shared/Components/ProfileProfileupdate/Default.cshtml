@model WCMS.WebSystem.WebParts.Integration.ViewComponents.ProfileProfileupdateViewModel
@* Ported from ProfileUpdate.ascx (Themes/area51/Parts/Profile/Profile). *@

<div class="component-profileprofileupdate">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Profile Update</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Profile Update component requires configuration.
        </div>
    }
</div>
