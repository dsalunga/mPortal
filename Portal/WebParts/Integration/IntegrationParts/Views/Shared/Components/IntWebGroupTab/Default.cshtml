@model WCMS.WebSystem.WebParts.ViewComponents.IntWebGroupTabModel

<div class="card" id="int-web-group-tab-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Group Tab</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Group tab control placeholder content.</p>
    </div>
</div>
