@model WCMS.WebSystem.WebParts.Integration.ViewComponents.GroupusermanagerViewModel
@* Ported from GroupUserManager.ascx (Apps/Integration/MasterList). *@

<div class="component-groupusermanager">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Group User Manager</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Group User Manager component requires configuration.
        </div>
    }
</div>
