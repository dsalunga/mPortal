@model WCMS.WebSystem.WebParts.Integration.ViewComponents.RegistrationConfigexternalmembersViewModel
@* Ported from ConfigExternalMembers.ascx (Apps/Integration/Registration). *@

<div class="component-registrationconfigexternalmembers">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Config External Members</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Config External Members component requires configuration.
        </div>
    }
</div>
