@model WCMS.WebSystem.WebParts.ViewComponents.IntWebGroupListModel

<div class="card" id="int-web-group-list-@Model.ObjectId">
    <div class="card-header">
        <h5 class="card-title mb-0">Web Group List</h5>
    </div>
    <div class="card-body">
        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <div class="alert @(Model.IsSuccess ? "alert-success" : "alert-danger")" role="alert">
                @Model.Message
            </div>
        }
        <p>Web group listing placeholder content.</p>
    </div>
</div>
