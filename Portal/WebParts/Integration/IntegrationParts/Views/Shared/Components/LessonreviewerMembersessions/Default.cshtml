@model WCMS.WebSystem.WebParts.Integration.ViewComponents.LessonreviewerMembersessionsViewModel
@* Ported from MemberSessions.ascx (Apps/Integration/LessonReviewer). *@

<div class="component-lessonreviewermembersessions">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Member Sessions</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Member Sessions component requires configuration.
        </div>
    }
</div>
