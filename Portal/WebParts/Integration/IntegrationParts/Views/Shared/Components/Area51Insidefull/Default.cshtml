@model WCMS.WebSystem.WebParts.Integration.ViewComponents.Area51InsidefullViewModel
@* Ported from InsideFull.ascx (Themes/area51). *@

<div class="component-area51insidefull">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Inside Full</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Inside Full component requires configuration.
        </div>
    }
</div>
