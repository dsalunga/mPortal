@model WCMS.WebSystem.ViewComponents.WebIdentitiesViewModel

<div class="card admin-panel">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-globe"></i> Site Identities</h5>
        <button type="button" class="btn btn-sm btn-success" id="btnAddIdentity" data-site-id="@Model.SiteId"><i class="bi bi-plus-lg"></i> Add Identity</button>
    </div>
    <div class="card-body p-0">
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger m-3">@Model.ErrorMessage</div>
        }
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Domain</th>
                        <th>Protocol</th>
                        <th>SSL</th>
                        <th>Default</th>
                        <th>Active</th>
                        <th>Modified</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Identities.Count == 0)
                    {
                        <tr><td colspan="7" class="text-center text-muted">No identities defined.</td></tr>
                    }
                    @foreach (var id in Model.Identities)
                    {
                        <tr>
                            <td><i class="bi bi-globe"></i> @id.DomainName</td>
                            <td>@id.Protocol</td>
                            <td>
                                @if (id.SslEnabled)
                                {
                                    <span class="badge bg-success">SSL</span>
                                }
                                else
                                {
                                    <span class="badge bg-warning text-dark">No SSL</span>
                                }
                            </td>
                            <td>@(id.IsDefault ? "✓" : "")</td>
                            <td>
                                @if (id.IsActive)
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Inactive</span>
                                }
                            </td>
                            <td>@id.ModifiedDate?.ToString("g")</td>
                            <td>
                                <button type="button" class="btn btn-sm btn-outline-primary btn-edit-identity" data-identity-id="@id.Id"><i class="bi bi-pencil"></i></button>
                                <button type="button" class="btn btn-sm btn-outline-danger btn-delete-identity" data-identity-id="@id.Id"><i class="bi bi-trash"></i></button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
