@model WCMS.WebSystem.ViewComponents.WebThemesViewModel

<div class="card admin-panel" id="pnlThemeEditor">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-palette"></i> Themes</h5>
        <button type="button" class="btn btn-sm btn-success" id="btnAddTheme"><i class="bi bi-plus-lg"></i> New Theme</button>
    </div>
    <div class="card-body p-0">
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger m-3">@Model.ErrorMessage</div>
        }
        <div class="table-responsive">
            <table class="table table-striped table-hover table-sm mb-0" id="gvThemes">
                <thead class="table-light">
                    <tr>
                        <th>Name</th>
                        <th>Description</th>
                        <th>Preview</th>
                        <th>Active</th>
                        <th>Modified</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Themes.Count == 0)
                    {
                        <tr><td colspan="6" class="text-center text-muted">No themes defined.</td></tr>
                    }
                    @foreach (var theme in Model.Themes)
                    {
                        <tr class="@(theme.Id == Model.SelectedThemeId ? "table-primary" : "")" data-theme-id="@theme.Id">
                            <td><i class="bi bi-palette2"></i> @theme.Name</td>
                            <td>@theme.Description</td>
                            <td>
                                @if (!string.IsNullOrEmpty(theme.PreviewUrl))
                                {
                                    <a href="@theme.PreviewUrl" target="_blank" class="btn btn-sm btn-outline-info"><i class="bi bi-box-arrow-up-right"></i> Preview</a>
                                }
                            </td>
                            <td>
                                @if (theme.IsActive)
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Inactive</span>
                                }
                            </td>
                            <td>@theme.ModifiedDate?.ToString("g")</td>
                            <td>
                                <button type="button" class="btn btn-sm btn-outline-primary btn-edit-theme" data-theme-id="@theme.Id" title="Edit"><i class="bi bi-pencil"></i></button>
                                <button type="button" class="btn btn-sm btn-outline-danger btn-delete-theme" data-theme-id="@theme.Id" title="Delete"><i class="bi bi-trash"></i></button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
