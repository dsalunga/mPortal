apiVersion: v1
kind: Service
metadata:
  name: mportal-web
  namespace: mportal
spec:
  selector:
    app: mportal-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
