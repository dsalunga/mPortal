@model BibleReader.ViewComponents.AccountAccountLoginViewModel
@* Ported from Login.aspx (Account). *@

<div class="component-accountaccountlogin">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Login</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Login component requires configuration.
        </div>
    }
</div>
