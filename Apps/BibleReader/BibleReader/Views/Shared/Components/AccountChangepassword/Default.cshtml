@model BibleReader.ViewComponents.AccountChangepasswordViewModel
@* Ported from ChangePassword.aspx (Account). *@

<div class="component-accountchangepassword">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Change Password</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Change Password component requires configuration.
        </div>
    }
</div>
