@model LessonReviewer.ViewComponents.ManageViewModel
@* Ported from Manage.aspx (Admin). *@

<div class="component-manage">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Manage</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Manage component requires configuration.
        </div>
    }
</div>
