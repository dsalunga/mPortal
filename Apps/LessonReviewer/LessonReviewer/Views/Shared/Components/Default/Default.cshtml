@model LessonReviewer.ViewComponents.DefaultViewModel
@* Ported from Default.aspx (). *@

<div class="component-default">
    @if (Model.ObjectId > 0)
    {
        <div class="component-content" data-object-id="@Model.ObjectId" data-record-id="@Model.RecordId">
            <p class="text-muted">
                <em>Default</em> — ported from legacy WebForms.
                Awaiting full UI implementation.
            </p>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            Default component requires configuration.
        </div>
    }
</div>
